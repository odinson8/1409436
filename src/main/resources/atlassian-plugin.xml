<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="resourceManagement"/>
    <!-- add our web resources -->
    <web-resource key="resourceManagement-resources" name="resourceManagement Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        <resource type="download" name="hideTextServiceDesk.js" location="/js/hideTextServiceDesk.js"/>
        <context>customerportal</context>
    </web-resource>

    <web-resource key="resourceManagement-resources2" name="resourceManagement Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        <resource type="download" name="refreshPage.js" location="/js/refreshPage.js"/>
        <context>customerportal</context>
    </web-resource>
</atlassian-plugin>